#ifndef STATE_$numero$
#define STATE_$numero$

#include "State_$numero$.h"


class State$numero$: public State
{
	public : 
		State$numero$();
		virtual ~State$numero$();
		bool transition(Automaton & automaton, Symbol * s);
};


#endif
